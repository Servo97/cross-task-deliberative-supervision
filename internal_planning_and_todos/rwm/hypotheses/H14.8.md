# H14.8 — Judge reasoning effort: max-effort labels → better encoder (user hypothesis)

**Origin (user, 2026-09-02, verbatim):** "We should run Qwen with effort max. Use the p5e node if
needed." then: "lets wait for pilot and then launch the full redo. If cross task deliberative
supervision is the main contribution that improves workspace models, we want the best model at
hand. But if the perf difference is negligible between low, medium, and max effort we can stick to
low or go with medium … Gauge the most sensible option forward."

**Claim (weakest form):** xhigh-effort judging changes the edge labels in ways that fix the known
label defects (CONTRAST precision 0.172 letter / 0.672 intent; planted-probe recovery 0.533) or
adds completion-condition content to pass-1 descriptors. Stronger (needs the redo + encoder cells):
the changed labels raise retrieval / policy metrics.

**Status:** OPEN. Decision rule pre-registered BEFORE the pilot (plan A17): R1 blind re-grade of the
40 low-vs-max disagreements — max right ≥60 % AND ≥10 more pairs than low (one-sided sign test);
R2 planted-CONTRAST recovery strict ≥0.60 / loose ≥0.75; R3 ≥50 % of pass-1 descriptors newly state
completion conditions. Any pass → full homogeneous redo; none → stay at low.

**Evidence:** prior κ(low, medium) = 0.838 (F4 PASS, no re-judge bought). Pass-1 low baseline 1,285
tok/segment, 0 truncations. Pass-2 xhigh pilot on the §15.3 150 paired buckets + 200 rcb anchors
(PILOT-2, `afb60016d29b8fc1`) and pass-1 xhigh on 100 RoboCasa segments (PILOT-1,
`2dc442271412b867`) — both RUNNABLE on p5 since 2026-09-02 15:10Z. Harness validated by
reproducing §15.3 exactly (12/17; probes 0.3556/0.5333) and caught a verdict-keying bug first (§46).

**Cost of the strong path:** redo = ALL 28,505 anchors in ONE homogeneous store (model, effort,
quantization never mixed inside a label artifact); 2-job split impossible under the 24 h cap → J-job
wave from PILOT-2's rate (2× → 5 jobs, 4× → 9, 5× → 11, 8× → 17); 77–153 node-h pass-2 (+13–26 if
pass-1 also); embed prefix `fullmine_4ee34e407ff4b71c` ready; entry refuses success unless counts
match and `_homogeneity.json` shows one (model, effort).

**Would move it:** pilots land → A17 harness → verdict (todos/T-judge-effort.md).

**2026-09-03 outcome:** both pilots FAILED without producing evidence — PILOT-2 on two harness
defects (`KeyError: 'strata'` in the pilot store; per-request client timeout too short for xhigh
chains) after burning its 15,870 s max_run; PILOT-1 on the frozen pass-1 token cap (8192 > 3072).
Status stays OPEN but **unmeasured**; coordinator recommendation = park at low (see T-judge-effort).
**Last updated:** 2026-09-03 17:20Z.
