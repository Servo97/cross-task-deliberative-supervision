# H14.7 — RoboMME: demo-prefix ω + live ω → GDN reproduces FrameSamp+Modul gains (user hypothesis)

**Origin (user, 2026-09-02, verbatim):** "For robomme, they do a version of frame stacking + MODUL
in the paper. I encourage you to read the paper and implement a feature parity workflow for
workspace model integration. i.e. the demo shown in the beginning maybe could be learned by the
workspace tokens and concatted to the live workspace tokens and together used with GDN to modify
the VLA actions." Paper: arXiv:2603.04639 — FrameSamp (≤32 uniform prefix frames → 512 tokens) +
Modul (cross-attention before FFN); teacher 368/800 = 46.00 % vs released π0.5 19.125 %.

**Claim (weakest form):** a fixed-slot window [8 demo-ω ; 16 live-ω] (K=24) into the GDN read →
AdaLN improves fixed-800 (h20/e16, model_seed 7) over live-only ω (M1, K=16) and over the same
recipe with structure-free ω (M3-ctrl), on the 9/16 tasks that carry a demo (900 episodes).
Counting tasks have no demo (0/4) → demo-axis bounded null pre-registered there.

**Status:** OPEN. Method card + ablation tree in `sep_02/robomme_wsm_parity.md`. Two pre-GPU
defects found and fixed (D1, D2). Tap jobs (encoder store + serve-aligned policy store) FAILED
2026-09-03 00:30Z on a missing `robocasa` module in the node venv → entry fixed, refire (T-tap).

**Evidence so far:**

| item | result | where |
|---|---|---|
| E0 anchor (M0 = v4_s0 base, 60k) | 143/800 = 17.875 % [15.38, 20.68]; −1.25 pp vs released base (bounded null, MDE 6.1); all 4 suites inside MDE | CAMPAIGNS.md §W4 |
| D1 | ω-store grid arange(0,n,8) vs serve frames ≡ exec_start_idx (mod 8) → live coverage 0 on 81 % of demo episodes → serve-aligned grid for the policy store (`tapserve`) | parity A1 |
| D2 (paper finding) | at the sealed `pos_decay_bias` init 0.0 the 8-slot demo prefix moves the conditioning vector 7e-6 (< fp16 floor) → M3 would be mechanically M1; init −4.0 on all four arms (prefix sensitivity 0.586 at init) | parity A2, A6–A11 |
| A2 fork diff verified | policy archive `445d9902…` (3 files); tap archive needs nothing | parity A6–A8 |

**Arms:** M0-70k base curve (fired), M1 live16, M2 demo8, M3 demo8+live16, M3-ctrl (ctrl-0b ω);
primary M3−M1 and M3−M3-ctrl, MDE 5 pp; recipe v4_70k (H14.9). Gate order: tapserve → ω export
(E1b-4tap) → D7 stored parity → prefix-ablation PASS on an M3 canary → G-flow/shape/prefix.

**Stronger variants refused for now:** "raw prefix frames (M4) would do the same" — not built (FS
canaries died on a JAX runtime split); "segment identity needs new parameters" — positional slots
suffice at −4.0 init (fallback learned [2,w] embedding pre-registered).

**Would move it:** M3 vs M1 vs M3-ctrl fixed-800 under the paper protocol (T-policy-arms).
**Last updated:** 2026-09-03.
