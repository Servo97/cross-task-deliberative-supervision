# H14.4 — ω carries memory content a history read can use (decodability)

**Claim tested:** ω (any trained cell) linearly or sequentially decodes memory-shaped labels that
a Markovian policy would need: bound slots after reveal, ReMemBench hidden sides, task progress
state. **Weakest form supported:** ω as trained carries no progress state beyond a normalized-time
clock; layout-type labels are perception (visible from frame 0).

**Origin:** the Markovianization certificate (plan §0.0; A13e progress gate; A15/A16).

**Status:** CLOSED-NEGATIVE (2026-08-29). Surviving reading H_absent. Pre-registered next cell E3
(event-marked ω) NOT run.

**Evidence (status §§14.8, 17.4.1, 19.4, 19.5):**

| probe | result | reading |
|---|---|---|
| RoboCasa bound slots, before vs after reveal | `before` tracks `after` on every slot; 357/750 episodes have an empty before-window | perception |
| rmb hidden sides (return_side, olive_side) | raw frozen per-frame tap decodes them at use time (0.664 / 0.588, LB above chance) | perception — slots DROPPED by the rule registered before the numbers |
| progress state, 8 families, 107k frames, pooled-linear | 0/8 beat the time-only baseline; untrained ≥ E1b in 6/8; ctrl-0b worst 7/8 | absent |
| same labels, causal GRU-64 (the GDN's form) | 0/8; E1b below time-only 8/8; ≤ its own label-shuffled positional floor 4/8 | absent; recurrent probe overfits (48–120 train episodes/fold vs 512-d input) |

The progress class is the first clean perception control (raw_tap·frame ≤ time-only 8/8), so the
label is memory-shaped and the miss is about ω. Mechanism reading: progress lives in EVENTS and no
term in {JEPA, SIGReg, edge SupCon} asks ω to mark them.

**Stronger variants refused:** "probe too weak" (GRU loses to a 2-d clock) → discriminator E3;
"H_nonlinear" unsupported but bounded only in this sample regime.

**Would move it:** todos/T-e3.md — per-frame segment-boundary / subskill-completion target from
the frozen segmentation (free labels), retrain, re-run §19/§19.5. Cost ≈ one local 5090 cell.
**Last sealed:** 2026-08-29.
